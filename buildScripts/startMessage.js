// We've created this file as we want following message to be displayed when we're starting our dev evironment/running server
//var chalk = require("chalk");               //chalk is a library that let us display messages in console in different colors (Standard js)

import chalk from 'chalk';
console.log(chalk.green("Starting app in dev mode.."));
